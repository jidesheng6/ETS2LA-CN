---
description: 【强制执行】编译、发布与开发环境规范
alwaysApply: true
---

# 【强制执行】编译与开发规范

## 环境要求

- .NET SDK **10.0+**（当前已验证 10.0.300）
- Git（含 submodule：`git clone --recurse-submodules`）
- Windows x64

## 编译方式

### 快速编译（Debug / IDE）

```powershell
dotnet build ETS2LA.sln -c Release
```

或在 VS Code / Visual Studio 中打开 `ETS2LA.sln`，按 **F5** 调试运行。

### 完整发布（自包含）

```powershell
.\publish.bat
```

等价于：

```powershell
dotnet build ETS2LA.sln -c Release --no-incremental
dotnet publish ETS2LA/ETS2LA.csproj --self-contained -o .\publish
xcopy /E /I /Y .\Assets .\publish\Assets
```

产物目录：`publish\ETS2LA.exe`

**强制：** 用户实际运行的是 `publish\ETS2LA.exe`。只执行 `dotnet build` 不会更新该目录。改完 UI / 版本号 / 覆盖层后必须跑 `.\publish.bat`，否则用户仍看到旧的 8 月 15 日产物。

`publish.bat` 必须备份并还原以下运行时目录（不在 git 内）：

| 目录 | 用途 |
|------|------|
| `publish\Plugins` | 已安装插件 DLL（如 tumppi066.laneassist） |
| `publish\Libraries` | 已安装库 DLL（如 tumppi066.pathlib） |

Windows 上 `PluginHandler.PluginRootPath` 为空，加载路径就是进程工作目录下的 `Plugins` / `Libraries`，即上述两处。

## 版本号

- 上游基线：`3.4.33`
- 中文 fork 版本：`3.4.33-zh.x`（见 `CHANGELOG.md` 与 `ETS2LA.csproj`）

## 优化 DLL（可选）

README 中蓝奏云链接的优化 DLL 不在源码仓库内，需手动下载后放入 `publish\Plugins` 或 `publish\Libraries`。
